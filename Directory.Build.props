<Project>
  <!--
    Directory.Build.props automatically applies to all projects in this directory and subdirectories.
    This ensures consistent build properties across the entire solution without requiring manual imports.
  -->
  <Import Project="$(MSBuildThisFileDirectory)Build\Common.props" Condition="Exists('$(MSBuildThisFileDirectory)Build\Common.props')" />
</Project>

